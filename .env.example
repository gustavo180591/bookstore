# Public environment variables (exposed to the client)
PUBLIC_UPLOADS_URL=http://localhost:5174/uploads

# Private environment variables (server-side only)
UPLOAD_DIR=static/uploads
MAX_FILE_SIZE=5242880 # 5MB
